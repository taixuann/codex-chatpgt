# Agent boundaries

The canonical portable agent-boundary reference is
[`../../architecture/agents.md`](../../architecture/agents.md). This subject-oriented
pointer exists so readers can discover it under `architecture/` without
duplicating or silently forking its content.

Canonical role identity comes from the external deployment registry when
available; repository guidance remains the portable semantic reference.
