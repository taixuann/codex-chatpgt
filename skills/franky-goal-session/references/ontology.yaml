schema: franky.goal-session.ontology
version: 1
extends:
  stable_id: /Users/tai/ai-labs/ops/standards/stable-id-navigation.md
  node_metadata: /Users/tai/ai-labs/ops/standards/node-metadata-spec.md
stable_id:
  goal: GOAL-YYYYMMDD-NNN
  role: AGENT-<ROLE>
  workflow: WF-<DOMAIN>-<NNN>
  task: TASK-NNN
  run: RUN-NNN
node_metadata:
  required: [id, stable_id, kind, version, owner_role, workflow_id, inputs, outputs, validation, approval_gate, on_failure]
  kinds: [goal, role, workflow, task, run, artifact, revision]
  failure_values: [return_to_human]
relations:
  - contains
  - supersedes
  - current_revision
  - owned_by
  - validated_by
