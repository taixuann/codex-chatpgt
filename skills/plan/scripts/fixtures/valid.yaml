schema_version: 1
kind: plan_packet
source:
  kind: intent_packet
  locator: intent-packet:fixture
  confirmed: true
  intent_source:
    packet_schema_version: 1
    kind: user
    locator: conversation
scenario: planning-and-task-breakdown
capabilities: [planning-and-task-breakdown]
objective: Validate the plan family structure.
assumptions: []
dependencies: []
tasks:
  - id: T1
    title: Define the bounded packet
    acceptance:
      - Packet schema is explicit.
    verification:
      - Run the packet validator.
    depends_on: []
checkpoints:
  - Packet validation passes.
out_of_scope:
  - Implementing the task.
open_questions: []
approved: true
side_effects: none
