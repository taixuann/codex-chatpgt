# Core Frontend and UI

1. What rendering model and state ownership fit the existing application?
2. What must users see during loading, empty, success, and failure states?
3. How are inputs validated and duplicate submissions prevented?
4. What list size, browser, device, and performance limits matter?
5. Can keyboard, screen-reader, low-vision, and reduced-motion users complete the flow?
6. What server/client contract could drift or expose unsafe content?

Verify the primary flow, error recovery, responsive layout, keyboard access, and production build.

