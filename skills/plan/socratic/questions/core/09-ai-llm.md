# Core AI, LLM, and Agent Systems

1. Does the task require an LLM, and what is the cost of a wrong output?
2. Which model/version is used, and what fallback, budget, and latency limits apply?
3. Are prompts, context, user input, and structured outputs separated and versioned?
4. What tools can act, which effects require approval, and what bounds loops and spend?
5. How are external content, retrieval, memory, and tool results treated as untrusted?
6. Which fixed evals, baselines, hard cases, and production failures measure quality?

Verify adversarial input, provider errors, tool denial, loop limits, eval regression, and measured token cost.

