# Core Backend and Services

1. Is a modular monolith sufficient, and which logic owns each invariant?
2. Which operations are synchronous, asynchronous, or stateful?
3. What are the timeout, retry, idempotency, and partial-failure policies?
4. How are inbound payloads and configuration validated?
5. What happens under concurrency, dependency failure, and resource exhaustion?
6. Can failed work be safely observed, replayed, or cancelled?

Verify malformed input, duplicate requests, dependency failure, missing configuration, and representative load.

