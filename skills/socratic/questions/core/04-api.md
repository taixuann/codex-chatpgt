# Core API, SDK, and Connectors

1. Who consumes the contract, and how is it versioned?
2. What are the request, response, pagination, and stable error shapes?
3. How are authentication, authorization, payload limits, and rate limits enforced?
4. Which operations require idempotency, asynchronous jobs, or signed webhooks?
5. How does each connector behave when unavailable, rate-limited, or changed?
6. Can a new consumer complete one successful integration from the documentation?

Verify schemas, invalid authentication, retry behavior, rate limits, idempotency, and examples.

