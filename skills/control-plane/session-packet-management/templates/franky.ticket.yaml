# Copy into the packet and replace the example values before execution.
# Validate against the active task contract.
kind: franky.task.v1
version: 1
contract_version: {major: 1, minor: 1}
accepted_result_versions: [franky.result.v1]
request_id: FRANKY-20260826-SESSION-001
objective: Perform the bounded, approved control-plane task described by this session packet.
scope:
  repository: taixuann/codex-chatpgt
  primary_targets: [skills/control-plane/session-packet-management]
  excluded_targets: [linked project contents, credentials, runtime/private sessions]
intent: {mode: audit, completion: bounded}
authority:
  mutation: approval_required
  external_effects: denied
  architecture_change: proposal_only
authority_matrix:
  franky_can: [inspect_control_plane, propose_changes, modify_allowed_control_plane_files, run_validators, produce_acceptance_ready_evidence]
  franky_cannot: [final_accept_own_change, modify_global_policy_without_review, change_agent_roles_without_decision, alter_validation_requirements, approve_runtime_security_changes]
required_capabilities: [control-plane-audit]
constraints:
  - Preserve canonical Issue/PLAN/PR authority.
  - Do not index credentials, linked projects, or runtime/private sessions.
done_criteria: [Scope and evidence are recorded in the session packet.]
required_outcomes: [Bounded findings and reproducible validation output.]
acceptance_requirements: [Deterministic validation and parent/human acceptance.]
stop_conditions: [Missing approval, scope conflict, protected path, or unavailable required evidence.]
review: {required: false, reason: Read-only bounded audit; parent decides whether additional review is needed.}
