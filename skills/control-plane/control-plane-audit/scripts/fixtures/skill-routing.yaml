version: 1
scope: tracked-control-plane-skills
cases:
  - id: bootstrap-project
    kind: positive
    prompt: Set up a new scientific file-first project with metadata and analysis folders.
    expected: project-bootstrap
    neighbors: [instruction-maintenance]
  - id: guidance-only
    kind: negative
    prompt: Update AGENTS.md so analysis scripts preserve raw data.
    expected: instruction-maintenance
    neighbors: [project-bootstrap]
  - id: link-existing-project
    kind: neighbor
    prompt: Link this existing project into the Codex workspace without copying its contents.
    expected: none
    neighbors: [project-bootstrap]
  - id: workflow-validation-only
    kind: negative
    prompt: Validate this workflow YAML and report schema errors.
    expected: none
    neighbors: [control-plane-audit]
  - id: persisted-workflow-design
    kind: negative
    prompt: Design a governed workflow with persisted gates and recovery for a scheduler consumer.
    expected: none
    neighbors: [control-plane-audit]
  - id: ordinary-coding
    kind: none
    prompt: Fix the parser bug in this application and run its unit tests.
    expected: none
    neighbors: [external-handoff, project-bootstrap]
  - id: ambiguous-setup
    kind: ambiguous
    prompt: Improve the setup around this project.
    expected: none
    neighbors: [project-bootstrap, instruction-maintenance]
  - id: intake-ambiguous-build
    kind: positive
    prompt: Build a production-grade context resolver for our control plane.
    expected: interview-me
    neighbors: [idea-refine, socratic, spec-driven-development]
  - id: intake-trivial-change
    kind: negative
    prompt: Rename this local variable and run the focused test.
    expected: none
    neighbors: [interview-me, planning-and-task-breakdown]
  - id: intake-scoped-plan
    kind: neighbor
    prompt: Implement the already-approved PLAN task exactly as written.
    expected: planning-and-task-breakdown
    neighbors: [interview-me, spec-driven-development]
  - id: prompt-rendering-only
    kind: negative
    prompt: Render this complete task contract for a read-only executor without changing it.
    expected: none
    neighbors: [interview-me, planning-and-task-breakdown]
  - id: raw-idea
    kind: neighbor
    prompt: Explore a vague idea without turning it into an Issue yet.
    expected: idea-refine
    neighbors: [interview-me, spec-driven-development]
