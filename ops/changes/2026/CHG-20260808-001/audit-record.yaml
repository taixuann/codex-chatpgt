schema: franky.audit
version: 1
audit_id: AUD-20260808-001
component_type: maintenance
component_id: codex-first-agent-routing
operation: audit_and_preview_setup
scope: >-
  /Users/tai/.codex/agents, /Users/tai/.codex/AGENTS.md,
  /Users/tai/.codex/config.toml, /Users/tai/.codex/hooks.json, and
  /Users/tai/.codex/workflows/franky only; no linked projects, credentials,
  .system, session data, remotes, or external harnesses.
source: /Users/tai/.codex
destination: /Users/tai/.codex
inputs:
  declared: [agent adapters, scoped guidance, canonical Franky workflow, local validators]
  resolved:
    - agents/code_mapper.toml
    - agents/codex_agent_template.toml
    - agents/feynman.toml
    - agents/franky.toml
    - agents/prometheus.toml
    - AGENTS.md
    - workflows/franky/franky.yaml
  valid: true
outputs:
  declared: [audit findings, exact setup preview, installed adapter set, memory integration, runtime-probe contract]
  consumers: [human approver, Codex parent agent]
  valid: true
cache: {mode: no-cache, path: null, valid: true}
protected_paths: {touched: [], rejected: [.system, credentials, linked-projects, sessions, remotes]}
collisions:
  - path: agents/codex_agent_template.toml
    finding: The file declares name pr_explorer, which violates the local filename/name validator.
    disposition: Preserve until the human approves one explicit resolution.
dependencies:
  - Canonical roles remain in /Users/tai/ai-labs/ops/agents/agents.yaml.
  - Franky workflow WF-FRANKY-CANONICAL governs control-plane changes.
  - GPT-5.6 model availability must be verified in the active Codex runtime before any adapter migration.
  - Agentmemory MCP and lifecycle hooks are supplied by the installed agentmemory 0.9.28 plugin.
overview:
  impact: >-
    No runtime configuration has changed. The prospective setup would affect only
    Codex role adapters and scoped routing guidance; it does not alter canonical
    roles, skills, workflows, project contents, or any external harness.
  references: [AGENTS.md, agents/README.md, workflows/franky/franky.yaml]
preview:
  changed_paths:
    - /Users/tai/.codex/agents/argus.toml
    - /Users/tai/.codex/agents/athena.toml
    - /Users/tai/.codex/agents/feynman.toml
    - /Users/tai/.codex/agents/prometheus.toml
    - /Users/tai/.codex/agents/franky.toml
    - /Users/tai/.codex/agents/AGENTS.md
    - /Users/tai/.codex/agents/CHANGELOG.md
    - /Users/tai/.codex/agents/templates/agent.toml
    - /Users/tai/.codex/agents/code_mapper.toml
    - /Users/tai/.codex/agents/codex_agent_template.toml
  digest: sha256:codex-agent-routing-memory-20260808-v3
rollback: {location: /Users/tai/.codex/ops/changes/2026/CHG-20260808-001/change.yaml, reversible: true}
status: pass
findings:
  - healthy: Canonical Franky entrypoint and all active adapters plus inert template validate.
  - resolved: Argus replaces the legacy code_mapper adapter; Athena supplies independent read-only review.
  - resolved: Feynman and Prometheus use GPT-5.6 Terra defaults; Franky uses GPT-5.6 Luna medium while retaining control-plane restrictions.
  - resolved: The invalid untracked template was converted into inert templates/agent.toml.
  - resolved: Agentmemory MCP was already wired; Codex hooks were merged with reversible external backups and the local REST listener is present.
  - warning: Startup health was initially reported healthy, but a later CLI check reported unknown health; agentmemory has zero sessions/observations until a new Codex session runs the lifecycle hooks.
  - warning: The worktree still contains unrelated pre-existing changes; they were not modified.
