# Claude Code source surfaces

Inspect only the selected source root. Common surfaces include `CLAUDE.md`,
`.claude/commands/`, `.claude/skills/`, `.claude/agents/`, `.mcp.json`,
`.claude/settings.json`, and `.claude.json`.

Map instructions, skills, commands, agents, supported hooks, and MCP servers
to the canonical migration categories. Treat plugins, marketplaces,
unsupported hooks, and provider-specific settings as manual review.
